/**
 * App — LACON Desktop entry point
 *
 * Renders the unified LaconWorkspace that integrates all writer-harness features.
 */

import React from 'react'

import { LaconWorkspace } from './components/LaconWorkspace'

function App() {
  return <LaconWorkspace />
}

export default App
