/**
 * AIDetectionPanel — Re-export from detection/ sub-module
 *
 * The panel has been refactored into focused sub-components:
 *   detection/AIDetectionPanel.tsx — Main orchestrator
 *   detection/ScoreGauge.tsx        — Circular AI score gauge
 *   detection/ParagraphCard.tsx     — Per-paragraph analysis card
 *   detection/HumanizeResultCard.tsx — Before/after rewrite card
 */

export { AIDetectionPanel } from './detection/AIDetectionPanel'

